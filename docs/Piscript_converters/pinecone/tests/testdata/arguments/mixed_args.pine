// Test mixing positional and named arguments
// Pine Script allows positional arguments followed by named arguments,
// but once you use a named argument, all following must be named
// Note: plot() now returns void and stores output internally

// Positional for required, named for optional
plot(100.0, title="Mixed", linewidth=2.0)
log.info("Plot 1 created")

// First two positional, rest named
plot(200.0, "Second Plot", linewidth=3.0, style="histogram")
log.info("Plot 2 created")

// Multiple positional, then named
plot(300.0, "Third", na, trackprice=true, offset=10.0)
log.info("Plot 3 created")

// Expected output:
// Plot 1 created
// Plot 2 created
// Plot 3 created
