//@version=4

// Multi-statement if block
if close > open
    x = 1
    y = 2
    z = 3

// Multi-statement if-else blocks
if close > open
    a = 1
    b = 2
else
    a = -1
    b = -2

// Multi-statement for loop
for i = 1 to 5
    sum := sum + i
    count := count + 1
