//@version=4

// Nested if and for statements
x = 10

if x > 5
    total = 0
    for i = 0 to 10
        if i % 2 == 0
            total := total + i
        else
            total := total - i
    result = total
